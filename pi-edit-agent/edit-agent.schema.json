{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "pi-edit-agent config",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "continuePrompt": {
      "type": "string",
      "description": "User message sent to trigger the continuation turn after editing an assistant message.",
      "default": "I edited your previous message. Continue from there."
    },
    "defaultAction": {
      "type": "string",
      "enum": ["continue", "editOnly"],
      "description": "Action taken when you press Esc on the 'what next?' prompt.",
      "default": "continue"
    }
  }
}
