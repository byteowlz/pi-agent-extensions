# Changelog

## 1.0.0

- Initial release.
- Adds the `/edit-agent` command: pick an assistant message on the current
  branch, edit its text, branch from its parent with the edited text, and
  optionally have the agent continue from the edit.
